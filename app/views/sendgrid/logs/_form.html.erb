<%= form_for(@sendgrid_log) do |f| %>
  <% if @sendgrid_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sendgrid_log.errors.count, "error") %> prohibited this sendgrid_log from being saved:</h2>

      <ul>
      <% @sendgrid_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
